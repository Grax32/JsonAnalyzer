{
  "name": "jsonanalyzer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prestart": "npm run build",
    "build": "tsc && browserify js-intermediate/json-analyzer.js -o dist/js/json-analyzer.js",
    "tsc-init": "tsc --init",
    "publish": "git add . && git commit -m publish && git push && npm publish"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^17.0.0",
    "typescript": "^5.0.4"
  }
}
